# VetRAG 兽医 RAG 系统 - 依赖清单
# 安装：pip install -r requirements.txt

# ========== 核心依赖 ==========
# Web 服务
fastapi>=0.110.0
uvicorn[standard]>=0.30.0

# 日志
loguru>=0.9.0

# 向量数据库
chromadb>=0.4.22

# Embedding 模型
sentence-transformers>=2.5.0

# LLM 模型（Qwen3）
transformers>=4.40.0
torch>=2.0.0

# 数据处理
numpy>=1.24.0

# ========== 微调相关（可选，仅 finetune_steps 目录需要） ==========
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
bitsandbytes>=0.41.0

# ========== 开发 / 测试依赖 ==========
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
httpx>=0.27.0
ruff>=0.4.0
