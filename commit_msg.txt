feat: fix ChromaDB path in test script and normalize import paths

Full-chain test:
- Add src/test_full_chain.py: end-to-end pipeline test with per-stage timing
- Fix CHROMA_DIR: was loading from src/chroma_db (empty), now uses config.CHROMA_DIR
  (VetRAG/chroma_db with 858 docs)
- Fix report output path from VetRAG/VetRAG/... to VetRAG/

Import normalization (CRLF fix + consistent absolute imports):
- rag_interface.py, rag_pipeline.py, retrievers/__init__.py,
  hybrid_retriever.py, vector_store_chroma.py
